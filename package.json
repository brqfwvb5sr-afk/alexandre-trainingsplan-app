{
  "name": "alexandre-trainingsplan-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "check": "node --check app.js && node --check service-worker.js",
    "lint": "npm run check",
    "build": "npm run check"
  }
}
